# Copy to .env for local runs (never commit .env).
# SRS_DB=./srs.db          # path to the SQLite card box (prod: /data/srs.db)
# MCP_TRANSPORT=http        # or "stdio" for a local agent
# PORT=8000
# HOST=0.0.0.0
